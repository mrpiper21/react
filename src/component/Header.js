import React from 'react'

const Header = () => {
  const headerStyle = {
    backgroundColor: 'blue',
    color: '#fff'
  } 

  return (
    <header style={headerStyle}>
        <h1>Grecery list</h1>
    </header>
  )
}

export default Header